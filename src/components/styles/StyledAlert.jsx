import { Fade, styled } from '@mui/material';

const StyledAlert = styled(Fade)({
    width: "70%",
    margin: "auto",
    borderRadius: 10,
    display: "flex",
    alignItems: "center",
})

export default StyledAlert;