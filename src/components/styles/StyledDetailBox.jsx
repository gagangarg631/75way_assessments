import { styled, Box } from '@mui/material';

const StyledDetailBox = styled(Box)({
    position: 'relative', 
    width: '90%', 
    height: 140, 
    margin: '10px 0', 
    borderRadius: 2, 
    boxShadow: 3, 
    display: 'flex', 
    alignItems: 'center',
    padding: 2,
})

export default StyledDetailBox;